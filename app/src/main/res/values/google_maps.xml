<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Google Maps API key used by the Listing Detail screen map preview.

      This value is injected at build time from root `secrets.properties` / `secrets.defaults.properties`
      via `app/build.gradle.kts` as a generated `resValue`.
    -->
</resources>

