<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Google Maps API key used by the Listing Detail screen map preview.

      When a real key is provided in secrets.properties / secrets.defaults.properties,
      the Gradle resValue override replaces this empty fallback at build time.
      This empty default ensures the build succeeds even without a key configured.
    -->
    <string name="google_maps_key" translatable="false"></string>
</resources>

