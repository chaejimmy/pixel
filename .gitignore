.DS_Store
Thumbs.db

# IntelliJ / Android Studio
.idea/
*.iml
*.ipr
*.iws

# Local SDK config
local.properties

# Local secrets (never commit)
secrets.properties

# Gradle
.gradle/
**/.gradle/

# Kotlin (K2 / compiler caches)
.kotlin/
**/.kotlin/

# Build outputs
build/
**/build/

# Android artifacts
captures/
*.apk
*.aab
*.keystore
*.jks
**/google-services.json

# Secrets properties (actual values — use secrets.properties.template as a starting point)
secrets.properties
secrets.local.properties

# Environment files
.env
.env.*
!.env.example

# JVM fatal error logs (Hotspot crash dumps) — local dev machine OOM artifacts,
# never useful on remote, and easily mistaken for production runtime crashes.
hs_err_pid*.log
replay_pid*.log
